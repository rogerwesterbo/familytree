apiVersion: v1
kind: Service
metadata:
  name: {{ include "familytreeweb.fullname" . }}
  labels:
    {{- include "familytreeweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "familytreeweb.selectorLabels" . | nindent 4 }}
