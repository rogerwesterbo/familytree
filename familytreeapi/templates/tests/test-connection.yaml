apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "familytreeapi.fullname" . }}-test-connection"
  labels:
    {{- include "familytreeapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "familytreeapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
